2013-06-20  Tri Kurniawan Wijaya  <tri-kurniawan.wijaya@epfl.ch>

There are three functionality supported by this project, performed by the three JAR files:

1. SpComputeModel.jar: compute cluster configuration from raw data (as the training data) 
for details, run: java -jar SpComputeModel -h

2. SpComputeSymbols.jar compute timeseries of symbols from cluster configuration file and raw data (as the testing data)
for details, run: java -jar SpComputeSymbols -h

3. SpGetRaw.jar convert time series of symbols to raw/real-values timeseries (using its cluster centroids)
for details, run: java -jar SpGetRaw -h

-----------------

